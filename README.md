# open-weather
This is a service that exposes a MCP endpoint to request 
weather information from
[Openweathermap](https://openweathermap.org).

The main purpose is to serve as a simple MCP server for MCP clients.

It requires an API Key from Openweathermap.

